# ==============================================
# AZURE OPENAI CONFIGURATION (REQUIRED)
# ==============================================
# Get these from your Azure OpenAI resource in Azure Portal
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-key-here
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Deployment names (must match your Azure OpenAI deployments)
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4

# ==============================================
# VECTOR DATABASE CONFIGURATION
# ==============================================
# Options: "chromadb" or "azure_search"
VECTOR_DB_TYPE=chromadb

# ChromaDB Settings (if VECTOR_DB_TYPE=chromadb)
CHROMADB_PATH=./data/chromadb

# Azure AI Search Settings (if VECTOR_DB_TYPE=azure_search)
# AZURE_SEARCH_ENDPOINT=https://your-search-service.search.windows.net
# AZURE_SEARCH_API_KEY=your-search-admin-key-here
# AZURE_SEARCH_INDEX_NAME=documents

# ==============================================
# OPTIONAL: AZURE BLOB STORAGE
# ==============================================
# For storing uploaded documents (optional, defaults to local filesystem)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string-here
# AZURE_STORAGE_CONTAINER_NAME=audit-documents

# ==============================================
# APPLICATION SETTINGS
# ==============================================
# Backend API settings
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
MAX_UPLOAD_SIZE_MB=100
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Database (SQLite for local, can use Azure SQL later)
DATABASE_URL=sqlite+aiosqlite:///./data/audit_app.db
